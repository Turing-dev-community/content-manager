{
  "upload_directory": "/tmp/files/",
  "output_directory": "/tmp/outputs/",
  "system_prompt": "You are an expert code reviewer. Analyze the provided codebase for bugs, security issues, and correctness problems. Provide actionable feedback. All files are located in /tmp/files",
  "task_prompt": "You are an expert code reviewer. Your task is to analyze the Content Manager Service codebase. This Spring Boot application manages blog posts, customer support requests, support responses, caching, exports, rate limiting, XML/CSV serialization, and security configuration. Identify subtle bugs, security regressions, data consistency issues, and logic flaws. Focus on issues detectable via static analysis. Write your findings to /tmp/outputs/review.md.",
  "rubric": [
    {
      "criterion": "Identifies the forbidden words lookup key mismatch in src/main/java/com/dehold/contentmanager/validation/step/ForbiddenWordValidator.java where content type is derived from getClass().getName() instead of simple name, preventing custom forbidden words from applying",
      "weight": 1
    },
    {
      "criterion": "Identifies the upsert collision bug in src/main/java/com/dehold/contentmanager/validation/repository/ValidationResultRepository.java where content_type was removed from the MERGE ON clause, allowing different content types to overwrite each other",
      "weight": 1
    },
    {
      "criterion": "Identifies the validation run deduplication bug in src/main/java/com/dehold/contentmanager/validation/service/ValidationServiceImpl.java in runSupportRequestValidation where a new runId is generated per result, breaking upsert semantics and causing duplicates",
      "weight": 1
    },
    {
      "criterion": "Identifies the case-sensitivity regression in src/main/java/com/dehold/contentmanager/validation/model/ContentTypeRegistry.java where contentType is no longer lowercased, causing unexpected fallbacks",
      "weight": 1
    },
    {
      "criterion": "Identifies the ValidationError field order swap in src/main/java/com/dehold/contentmanager/validation/model/ValidationError.java where the record now stores message before code, inverting error semantics",
      "weight": 1
    },
    {
      "criterion": "Identifies the contentId/userId swap in src/main/java/com/dehold/contentmanager/validation/model/ValidationResult.java where valid/invalid factory methods pass userId as contentId",
      "weight": 1
    },
    {
      "criterion": "Identifies the authentication query drift in src/main/java/com/dehold/contentmanager/config/SecurityConfig.java where usersByUsernameQuery uses email instead of username, breaking username-based login",
      "weight": 1
    },
    {
      "criterion": "Identifies the cache TTL regression in src/main/java/com/dehold/contentmanager/config/CacheConfig.java where expireAfterWrite uses seconds instead of minutes, causing rapid cache expiry and higher load",
      "weight": 1
    }
  ]
}
